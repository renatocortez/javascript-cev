function resultado() {
    let n1 = Number(window.prompt('Digite um número inteiro: '))
    mais = n1 ++
    menos = n1 --

    window.alert(`Antes de ${n1}, temos o número ${n1 - 1}. Depois de  ${n1}, temos o número ${n1 + 1}`)

}